import React from "react";
import ReactDOM from "react-dom";

import Formulario from "./public/formulario";

ReactDOM.render(
  <React.StrictMode>
    <div className="contenedor">
      <Formulario />
    </div>
  </React.StrictMode>,
  document.getElementById("root")
);
